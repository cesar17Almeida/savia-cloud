# Production environment -- install as /etc/savia-cloud/env (chmod 600, owner savia).
# Loaded by systemd (EnvironmentFile), NOT by the app itself.

TTN_BASE_URL=https://eu1.cloud.thethings.network
TTN_APP_ID=savia
TTN_API_KEY=            # AS API key with downlink rights

STATION_LAT=39.47
STATION_LON=-0.38

DATABASE_URL=sqlite:////var/lib/savia-cloud/savia.db

WEBHOOK_SECRET=         # X-Webhook-Token expected on POST /ttn/uplink
CRON_SECRET=            # X-Cron-Token expected on POST /cron/daily
CRON_DAILY_HOUR=6

MODEL_PATH=/opt/savia-cloud/models/lstm_hs30_int8_pt.tflite

HOST=127.0.0.1
PORT=8000
